import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "How It Works",
  description: "Learn how to start earning with SonkoPesa in three simple steps. Join, Share, and Earn with our premium referral platform.",
}

export default function HowItWorksLayout({ children }: { children: React.ReactNode }) {
  return children
}
